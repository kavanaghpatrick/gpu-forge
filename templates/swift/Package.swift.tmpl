// swift-tools-version: 5.9
// Package.swift — Metal compute project
// Generated by gpu-forge

import PackageDescription

let package = Package(
    name: "{{PROJECT_NAME}}",
    platforms: [
        .macOS(.v14)
    ],
    targets: [
        .executableTarget(
            name: "{{PROJECT_NAME}}",
            path: "Sources/{{PROJECT_NAME}}",
            resources: [
                .process("Shaders")
            ],
            linkerSettings: [
                .linkedFramework("Metal"),
                .linkedFramework("MetalKit"),
                .linkedFramework("Foundation")
            ]
        )
    ]
)
